import { Remult } from '../context';
import { EntityBase } from '../remult3';
export declare class c extends EntityBase {
    private remult;
    id: number;
    name: string;
    constructor(remult: Remult);
}
